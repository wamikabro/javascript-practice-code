const namee = "wamique";

console.log(namee); // simply printed the string

const newName = namee.replace("a", "e");

console.log(namee); // remained unchanged

console.log(newName); // new name was created

// that's why we were able to manipulate even const
