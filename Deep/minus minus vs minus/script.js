let string = "5";

string = string - 1; // 4 because - operator is converting it into a number
console.log(string);
string--; // 3, because -- is converting it into a number first
console.log(string);
